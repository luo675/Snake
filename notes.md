# 学习记录

这里记录学习 C++ 控制台贪吃蛇过程中的每一步内容、遇到的问题和解决方法。

## 第 1 步：确认 C++ 编译环境

### 本步目标
确认 Windows + VS Code + g++ / MinGW 环境可以编译 C++ 程序。

### 运行的命令
g++ --version

### 当前状态
g++ 可以正常运行，版本是 15.2.0。

### 下一步
创建第一个 C++ 文件，写出最小程序，并完成第一次编译运行。

## 第 2 步：创建并运行第一个 C++ 程序

### 本步目标
创建 main.cpp，确认可以编译并运行自己的 C++ 程序。

### 写了什么
写了最小的 C++ 程序，包含 include、main 函数、cout 输出和 return 0。

### 运行的命令
g++ main.cpp -o snake.exe
.\snake.exe

### 当前状态
程序可以成功编译和运行，终端输出 Snake game start!。

### 下一步
学习如何在控制台画出一个简单的游戏区域。

## 第 3 步：画出控制台游戏区域

### 本步目标
用两层 for 循环画出一个固定大小的控制台地图。

### 写了什么
使用 width 和 height 控制地图大小。
外层循环 y 控制行，内层循环 x 控制列。
通过 if 判断当前位置是否在边缘：
- 第一行
- 最后一行
- 第一列
- 最后一列

边缘输出 #，中间输出空格。

### 当前状态
程序可以输出一个 10x5 的空心矩形边框。

### 下一步
在地图中间放一个蛇头，用字符 O 表示。

## 第 4 步：在地图中显示蛇头

### 本步目标
用坐标变量控制蛇头显示在地图中的指定位置。

### 写了什么
新增 snakeX 和 snakeY 两个变量，表示蛇头的列坐标和行坐标。
在画地图时，如果当前 x 和 y 等于蛇头坐标，就输出 O。

### 学到什么
控制台地图可以看成一个二维坐标区域。
外层循环的 y 表示行，内层循环的 x 表示列。
通过比较 x、y 和某个对象的坐标，就可以决定在当前位置画什么字符。

### 当前状态
程序可以输出一个 10x5 的地图，并在中间显示蛇头 O。

### 下一步
尝试修改 snakeX 和 snakeY，观察蛇头位置如何变化。

## 第 1 步：确认 C++ 编译环境

  ### 本步目标
  确认 Windows + VS Code + g++ / MinGW 环境可以编译 C++ 程序。

  ### 运行的命令
  g++ --version

  ### 当前状态
  g++ 可以正常运行，版本是 15.2.0。
  创建第一个 C++ 文件，写出最小程序，并完成第一次编译运行。

## 第 2 步：创建并运行第一个 C++ 程序

  ### 本步目标
  创建 main.cpp，确认可以编译并运行自己的 C++ 程序。

  ### 写了什么
  写了最小的 C++ 程序，包含 include、main 函数、cout 输出和 return 0。

  ### 运行的命令
  g++ main.cpp -o snake.exe
  .\snake.exe

  ### 当前状态
  程序可以成功编译和运行，终端输出 Snake game start!。

  ### 下一步
  学习如何在控制台画出一个简单的游戏区域。

## 第 3 步：画出控制台游戏区域

  ### 本步目标
  用两层 for 循环画出一个固定大小的控制台地图。

  ### 写了什么
  使用 width 和 height 控制地图大小。
  外层循环 y 控制行，内层循环 x 控制列。
  通过 if 判断当前位置是否在边缘：
  - 第一行
  - 最后一行
  - 第一列
  - 最后一列

  边缘输出 #，中间输出空格。

  ### 当前状态
  程序可以输出一个 10x5 的空心矩形边框。

  ### 下一步
  在地图中间放一个蛇头，用字符 O 表示。

## 第 4 步：在地图中显示蛇头

  ### 本步目标
  用坐标变量控制蛇头显示在地图中的指定位置。

  ### 写了什么
  新增 snakeX 和 snakeY 两个变量，表示蛇头的列坐标和行坐标。
  在画地图时，如果当前 x 和 y 等于蛇头坐标，就输出 O。

  ### 学到什么
  控制台地图可以看成一个二维坐标区域。
  外层循环的 y 表示行，内层循环的 x 表示列。
  通过比较 x、y 和某个对象的坐标，就可以决定在当前位置画什么字符。

  ### 当前状态
  程序可以输出一个 10x5 的地图，并在中间显示蛇头 O。

  ### 下一步
  尝试修改 snakeX 和 snakeY，观察蛇头位置如何变化。  
