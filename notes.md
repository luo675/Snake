# 学习记录

这个文件要同时保留两件事：

- 以前学到哪里了
- 下一次从哪里继续

## 已完成的步骤

### 第 1 步：确认 C++ 编译环境

- 这一步要解决什么问题：确认 Windows + VS Code + g++ / MinGW 能不能编译 C++ 程序。
- 我写了什么：检查了 `g++ --version`。
- 当前代码状态：
  - 是否能编译：能
  - 是否能运行：能
  - 还有什么问题：暂无
- 我学到了什么：当前 g++ 可以正常使用，版本是 15.2.0。
- 下一次从哪里继续：创建第一个 C++ 文件，写最小程序并完成第一次编译运行。

### 第 2 步：创建并运行第一个 C++ 程序

- 这一步要解决什么问题：确认可以自己创建 `main.cpp` 并成功编译运行。
- 我写了什么：写了最小的 C++ 程序，包含 `include`、`main` 函数、`cout` 输出和 `return 0`。
- 当前代码状态：
  - 是否能编译：能
  - 是否能运行：能
  - 还有什么问题：暂无
- 我学到了什么：程序可以成功编译和运行，终端输出 `Snake game start!`。
- 下一次从哪里继续：学习如何在控制台画出一个简单的游戏区域。

### 第 3 步：画出控制台游戏区域

- 这一步要解决什么问题：用两层 `for` 循环画出固定大小的控制台地图。
- 我写了什么：使用 `width` 和 `height` 控制地图大小；外层循环控制行，内层循环控制列；通过判断当前位置是否在边缘决定输出 `#` 还是空格。
- 当前代码状态：
  - 是否能编译：能
  - 是否能运行：能
  - 还有什么问题：暂无
- 我学到了什么：控制台地图可以看成一个二维坐标区域，外层循环是行，内层循环是列。
- 下一次从哪里继续：在地图中间放一个蛇头，用字符 `O` 表示。

### 第 4 步：在地图中显示蛇头

- 这一步要解决什么问题：用坐标变量控制蛇头显示在地图中的指定位置。
- 我写了什么：新增 `snakeX` 和 `snakeY` 两个变量，表示蛇头的列坐标和行坐标；在画地图时，如果当前 `x` 和 `y` 等于蛇头坐标，就输出 `O`。
- 当前代码状态：
  - 是否能编译：能
  - 是否能运行：能
  - 还有什么问题：目前只能显示静止的蛇头，还不会移动
- 我学到了什么：控制台地图就是一个二维坐标区域，可以通过比较坐标决定当前位置该画什么字符。
- 下一次从哪里继续：尝试修改 `snakeX` 和 `snakeY`，观察蛇头位置如何变化。

## 当前继续点

### 第 5 步：让蛇头动起来

- 这一步要解决什么问题：让蛇头位置随着输入或循环变化，而不是固定不动。
- 我写了什么：把绘制地图放进 `frame` 循环里，每一帧先让 `snakeX` 加 1，再画一次地图。
- 当前代码状态：
  - 是否能编译：能
  - 是否能运行：能
  - 还有什么问题：`ateFood` 只是被记录了，还没有真正改变游戏状态
- 我学到了什么：把反向判断改成内边界后，移动看起来更自然了；再加 `dy` 后，蛇头可以在二维方向移动；再加食物坐标后，地图里可以同时显示蛇头和食物；用布尔变量可以先记录“是否吃到食物”。
- 下一次从哪里继续：如果吃到食物，就把食物移动到新位置，并准备做“增长”。

## 本次暂停点

- 当前已经做到：
  - 蛇头可以在地图里二维移动
  - 地图里可以显示食物 `*`
  - 可以判断蛇头是否碰到食物
  - `ateFood` 已经作为状态变量引入
- 还没有做：
  - 吃到食物后移动食物到新位置
  - 蛇身增长
  - 键盘输入控制方向
- 下次直接从这里开始：
  - 先让 `ateFood` 真正改变食物位置，再考虑增长

## 以后每次学习结束都这样记

- 这一步要解决什么问题
- 我写了什么
- 当前代码状态
  - 是否能编译
  - 是否能运行
  - 还有什么问题
- 我学到了什么
- 下一次从哪里继续
